# 数据库（本地默认即可，按需修改）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=maze_kill

# 服务端口
PORT=3000

# JWT密钥（上线务必更换）
JWT_SECRET=maze_kill_secret_2026

# 超管初始密码（仅种子脚本使用）
ADMIN_PASSWORD=admin888

# 微信小程序（上线后填写正式appid/secret，演示环境留空走降级openid）
WECHAT_APPID=
WECHAT_SECRET=
